import { useSelector } from "react-redux";

const Review = () => {
    const image = useSelector(store => store.reviewSlice);
    
  return (
    <>
    <h1></h1>
      
    </>
  );
};
export default Review;
